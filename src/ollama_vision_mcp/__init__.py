"""ollama-vision-mcp — minimal local vision bridge for VS Code Copilot.

Reads local image files and returns text descriptions produced by a local
Ollama vision model over its OpenAI-compatible HTTP API. This package is only
a bridge: it never installs, starts, or manages Ollama.
"""

__version__ = "0.1.0"
